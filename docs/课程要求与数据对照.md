# 课程/Proposal 与当前实验数据对照（CS5296 Spring 2026）

本文对照课程说明、Group 259 Proposal（EC2 vs Serverless、冷启动、时限、高并发等）与仓库内**已有结果**，并标明**缺口**与**可补测命令**（便于报告与演示）。

## 1. 评估维度（结课报告 / Artifact）

| 要求 | 当前状态 | 说明 |
|------|----------|------|
| 实现两条架构（EC2 + Lambda）+ 可跑制品 | 有 | 容器与 Function URL 已能完成 DOCX→PDF |
| 性能对比、观察与解释 | **基本满足（双链路）** | 含 **DOCX 全矩阵 + N=25 分位数 + 并发**，**PDF→图 全 6 条** 见 `results/lambda_pdf_matrix_20260423_162600.json` 与 `results/ec2_pdf_matrix_20260423_162630.json`、`docs/结论.md` §8 |
| 冷启动、15 分钟限、高并发、伸缩 | 部分满足 | **并发**有 3 路同发数据；**冷启动 30 分钟**未自动化；**15 分钟**在本合成数据上未触及 |
| 原始数据与可复现脚本 | 基本满足 | `results/*.json`、**benchmarks/*.sh**、**docs/benchmark-runbook.md**；**PDF** 已上传后补测完成（见上表与 `结论.md` §8） |
| GitHub 公开、多成员 commit 历史 | 需小组自查 | 不在本文验证范围 |

## 2. 与 Proposal/实验矩阵（experiment-matrix）逐条

| 矩阵项 | 是否已有数据 | 文件或说明 |
|--------|----------------|------------|
| 冷请求（≥30 min 空闲后首包） | **缺** | 需人为空闲 30 分钟后对 Lambda/EC2 各打首包，并记 CloudWatch `Init Duration`；命令见 `docs/metrics-definitions.md` |
| 突发/阶梯升压 | **部分** | 有 **3 路并发**（`results/lambda_concurrent3_docx_baseline_medium_*.json`）；**阶梯 RPS** 建议用 k6 在装好后补跑并保存 `.log` |
| 持续平台期 5–10 min | **部分** | 有 **N=25、间隔 0.5s** 的**稳态小样本**（非满 5–10 分钟整段）；在报告中注明**时间/成本限制**或补跑 k6 长时间段 |
| 因变量 p50/p90/p99 | **有（DOCX 单点）** | `lambda_docx_baseline_medium_n25_stats_*.json` 与 `ec2_docx_baseline_medium_n25_stats_*.json`（**同 id，便于写对照**） |
| Lambda `InitDuration` | **缺** | 从 CloudWatch Lambda 日志 `REPORT` 行拷贝 |
| API Gateway 29s | **未**经 **API Gateway** 测；当前为 **Function URL** | 报告中区分「API GW 同步限」与「FURL/直连」；若课程要求 APIGW 对比需再布一条或专门说明 |

## 3. 本轮新补充的数据（相对仅「6×1 矩阵」之前）

- **分位数（Client 端）**：`docx_baseline_medium`，**N=25**，Lambda 与 **EC2 实例内**各一轮；见 `results/lambda_docx_baseline_medium_n25_*.json`、`results/ec2_docx_baseline_medium_n25_stats_20260423_162200.json` 与 `ec2_docx_baseline_medium_n25_raw_ms.txt`。
- **EC2 重复脚本**：`benchmarks/ec2-docx-repeat.sh`（可改 N 与 id）。

## 4. 仍缺数据（需你或后续命令补齐）

1. **PDF 在 S3**：**已完成** upload；PDF→图 实验数据见 `结论.md` §8 与 `results/lambda_pdf_matrix_20260423_162600.json`、`ec2_pdf_matrix_20260423_162630.json`。
2. **冷启动 30 分钟 + Init Duration**：定闹钟后**人工**发首请求并截图 CloudWatch。
3. **5–10 分钟**持续 RPS / k6：安装 k6 后按 `docs/benchmark-runbook.md` 跑 `stages` 长阶段。

## 5. 是否「太少」的简要结论

- **对课设「有 EC2+Lambda+对比+可复现数据」的底线**：在补齐 **PDF→图**、**冷启一条** 与 **报告里写清口径** 后，**内容深度**更稳。  
- **对「高工作量」评分**（13 分内容）：建议至少 **(a)** PDF 全链路数据 **(b)** 分位数+并发 **(c)** 冷启或**明确讨论为何未测** **(d)** 若用 **Function URL** 与 **API Gateway** 的差异说明。

