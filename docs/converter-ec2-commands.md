# Converter?EC2????? — ???

?????`cs5296project/services/converter`?????? `services/converter`??

## 1. ??/????????????

????**??**?? `docker build`???? `Dockerfile`?`requirements.txt`?`entrypoint.sh`??**??**???

```bash
cd ~/cs5296project/services/converter   # ???????
export AWS_REGION=ap-southeast-1         # ???????

export IMAGE_NAME="cs5296-converter:ec2"
docker build -t "${IMAGE_NAME}" .
```

## 2. ??????? `src` ? — ???????

??? COPY ????? `src`??????**???????**??? `/app/src`???? `main.py` ?**?????**????? `docker build`?`pull` ? `git pull` ???????????

??????????????????????????

```bash
cd ~/cs5296project/services/converter
export AWS_REGION=ap-southeast-1
export IMAGE_NAME="cs5296-converter:ec2"

# ???????????????????/?????
docker rm -f g259ec2 2>/dev/null || true

docker run -d --name g259ec2 -p 8080:8080 -p 3000:3000 \
  -e "AWS_REGION=${AWS_REGION}" \
  -v "$(pwd)/src:/app/src" \
  --restart unless-stopped \
  "${IMAGE_NAME}"
```

???

- ????? **`services/converter/src`????? `/app/src`?** ??????????
- ???? `requirements.txt`????? `docker build` ?**??** `-v` ??????????????????? `pip install`???????????/EC2 ?????

## 3. ? GitHub ??????? EC2 ??

```bash
cd ~/cs5296project
git pull origin main
cd services/converter

# ????? 2. ???????????????? build?
docker rm -f g259ec2 2>/dev/null || true
export AWS_REGION=ap-southeast-1
export IMAGE_NAME="cs5296-converter:ec2"
docker run -d --name g259ec2 -p 8080:8080 -p 3000:3000 \
  -e "AWS_REGION=${AWS_REGION}" \
  -v "$(pwd)/src:/app/src" \
  --restart unless-stopped \
  "${IMAGE_NAME}"
```

## 4. ???????????

? `BUCKET`/`KEY` ???? S3 ???

```bash
curl -sS "http://127.0.0.1:8080/health"

curl -sS -w "\nhttp_code=%{http_code}\n" -X POST "http://127.0.0.1:8080/v1/convert/docx-to-pdf" \
  -H "Content-Type: application/json" \
  -d '{
  "input": { "bucket": "cs5296-project", "key": "test_word.docx" },
  "output": { "bucket": "cs5296-project", "keyPrefix": "output/pdf/" }
}'
```

## 5. ????????

```bash
docker logs g259ec2 2>&1 | tail -n 200
```

??????????????? `debug-fb6a05.log` ? `/app` ???????????????

```bash
docker cp g259ec2:/app/debug-fb6a05.log ./
```

## 6. ???Windows PowerShell??? — ????????

? `services\converter` ??????????????

```powershell
$env:AWS_REGION = "ap-southeast-1"
$env:IMAGE_NAME = "cs5296-converter:ec2"
docker rm -f g259ec2 2>$null; $null
docker run -d --name g259ec2 -p 8080:8080 -p 3000:3000 `
  -e "AWS_REGION=$($env:AWS_REGION)" `
  -v "${PWD}/src:/app/src" `
  --restart unless-stopped `
  $env:IMAGE_NAME
```

## 7. ?????? build ??

| ???? | ???? `docker build` |
|----------|-------------------------|
| ?? `src/*.py` ?? `-v` ?? | ????? `git pull` ?????? |
| `requirements.txt` | ? |
| `Dockerfile` / `entrypoint.sh` | ? |
| ????????? COPY ??? | ????????? build ????????? |
